import React from 'react';

const PageNotFound
    = props => {
    return (
        <div>
    page not found page
        </div>
    );
};

PageNotFound.propTypes = {

};

export default PageNotFound;